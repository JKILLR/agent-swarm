# Backend Environment Configuration
#
# Claude CLI OAuth Token for Max subscription users:
# Run `claude setup-token` in your terminal to generate a token,
# then copy the token value here.
#
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-YOUR_TOKEN_HERE

# Alternative: Use Anthropic API key (requires API credits)
# ANTHROPIC_API_KEY=sk-ant-api03-YOUR_API_KEY_HERE
