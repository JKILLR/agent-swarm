"""Agent execution API endpoints.

This module provides endpoints for agent execution including:
- Executing agents
- WebSocket for agent events

Note: These endpoints are placeholders that reference main.py.
Full extraction pending.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api/agents", tags=["agents"])

# Agent execution routes remain in main.py for now
# They will be migrated in a future refactoring phase
