"""File management API endpoints.

This module provides endpoints for file operations in swarm workspaces including:
- Listing files
- Reading files
- Creating/updating files
- Uploading files

Note: These endpoints are placeholders that reference main.py.
Full extraction pending.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api/swarms", tags=["files"])

# File routes are complex and remain in main.py for now
# They will be migrated in a future refactoring phase
