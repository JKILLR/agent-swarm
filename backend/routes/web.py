"""Web search and fetch API endpoints.

This module provides endpoints for web operations including:
- Web search
- URL fetching

Note: These endpoints are placeholders that reference main.py.
Full extraction pending.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api", tags=["web"])

# Web routes remain in main.py for now
# They will be migrated in a future refactoring phase
