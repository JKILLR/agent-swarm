"""Workflow API endpoints.

This module provides endpoints for workflow management including:
- Listing workflows
- Running workflows
- Workflow status

Note: These endpoints are placeholders that reference main.py.
Full extraction pending.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api/swarms", tags=["workflows"])

# Workflow routes remain in main.py for now
# They will be migrated in a future refactoring phase
